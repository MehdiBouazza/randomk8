apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: producteur
spec:
  selector:
    matchLabels:
      name: daemonset-index
  template:
    metadata:
      labels:
        name: daemonset-index
    spec:
      containers:
      - name: alpine
        image: alpine:3.13.4
        volumeMounts:
        - name: volumeWeb
          mountPath: /data/web
      volumes:
      - name: volumeWeb
        hostPath: 
          path: /data/web
          type: DirectoryOrCreate
